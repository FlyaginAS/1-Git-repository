'use strict'
alert( [] == [] ); // false
alert( [] == ![] ); // true
//объекты передаются по ссылке, объект равен только самому себе
//alert([] == false) -> alert(''==false) -> true массив это объект, объект это тру даже если пустой
